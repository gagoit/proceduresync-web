.panel.panel-default#user_devices_panel
  .panel-heading
    %h4 #{t("user.devices.title")}
    
  .panel-body.scroll-panel
    .table-responsive
      %table#table-user-devices.table.table-striped{:width => "100%", :'data-url' => devices_user_path(user), "data-see-remotely-wipe" => PermissionService.can_remotely_wipe_device(current_company, current_user, user).to_s}